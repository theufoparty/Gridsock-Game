export interface IUserType {
  username: string;
  color: string;
  id: string;
  isReady: boolean;
  points: number;
}

export interface IUserMessageType {
  user: string;
  message: string;
  color: string;
}
